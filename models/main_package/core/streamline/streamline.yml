version: 2
models:
  - name: streamline__chainhead
    description: "This model is used to get the chainhead from the blockchain."

    columns:
      - name: BLOCK_ID
        tests:
          - not_null